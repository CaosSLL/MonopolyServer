{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PosesionCasilla list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numcabania</th>
                <th>Hipotecada</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('posesioncasilla_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.numCabania }}</td>
                <td>{{ entity.hipotecada }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('posesioncasilla_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('posesioncasilla_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('posesioncasilla_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
