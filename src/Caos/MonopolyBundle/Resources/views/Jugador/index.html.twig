{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Jugador list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Posicion</th>
                <th>Dinero</th>
                <th>Carcel</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('jugador_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.posicion }}</td>
                <td>{{ entity.dinero }}</td>
                <td>{{ entity.carcel }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('jugador_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('jugador_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('jugador_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
