{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Jugador</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Posicion</th>
                <td>{{ entity.posicion }}</td>
            </tr>
            <tr>
                <th>Dinero</th>
                <td>{{ entity.dinero }}</td>
            </tr>
            <tr>
                <th>Carcel</th>
                <td>{{ entity.carcel }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('jugador') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('jugador_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
